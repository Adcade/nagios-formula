{% set map = salt['grains.filter_by']({
    'Arch': {
        'pkgs': ['nagios', ],
        'service': 'nagios',
        },
    'Debian': {
        'pkgs': ['nagios3', ],
        'service': 'nagios3',
    },

}, merge=salt['pillar.get']('nagios:lookup')) %}
